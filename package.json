{
  "name": "codex-lattice",
  "version": "0.2.1",
  "description": "Quality-first model and reasoning-effort orchestration for Codex GPT-5.6 Sol/Terra/Luna agents.",
  "type": "module",
  "bin": {
    "codex-lattice": "./bin/codex-lattice.js"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check bin/codex-lattice.js && node --check src/policy.js && node --check src/installer.js && node --check src/telemetry.js && node --check src/runtime.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "Apache-2.0",
  "keywords": [
    "codex",
    "openai",
    "ai-agents",
    "multi-agent",
    "agent-orchestration",
    "model-routing",
    "llm-router",
    "reasoning",
    "gpt-5.6"
  ]
}
